import { EintragListModel } from './eintrag-list-model';

describe('EintragListModel', () =>
{
    it('should create an instance', () =>
    {
        expect(new EintragListModel()).toBeTruthy();
    });
});
