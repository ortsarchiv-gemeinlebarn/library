import { ItemInternModel } from './item-intern-model';

describe('ItemInternModel', () =>
{
    it('should create an instance', () =>
    {
        expect(new ItemInternModel()).toBeTruthy();
    });
});
