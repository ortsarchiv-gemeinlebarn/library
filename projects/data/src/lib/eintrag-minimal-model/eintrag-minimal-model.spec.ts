import { EintragMinimalModel } from './eintrag-minimal-model';

describe('EintragMinimalModel', () =>
{
    it('should create an instance', () =>
    {
        expect(new EintragMinimalModel()).toBeTruthy();
    });
});
