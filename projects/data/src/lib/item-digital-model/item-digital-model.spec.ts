import { ItemDigitalModel } from './item-digital-model';

describe('ItemDigitalModel', () =>
{
    it('should create an instance', () =>
    {
        expect(new ItemDigitalModel()).toBeTruthy();
    });
});
