import { EintragSchaufensterModel } from './eintrag-schaufenster-model';

describe('EintragSchaufensterModel', () =>
{
    it('should create an instance', () =>
    {
        expect(new EintragSchaufensterModel()).toBeTruthy();
    });
});