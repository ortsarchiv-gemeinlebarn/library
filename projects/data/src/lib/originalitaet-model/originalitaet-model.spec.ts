import { OriginalitaetModel } from './originalitaet-model';

describe('OriginalitaetModel', () =>
{
    it('should create an instance', () =>
    {
        expect(new OriginalitaetModel()).toBeTruthy();
    });
});
