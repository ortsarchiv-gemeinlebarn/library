import { ArbeitslisteModel } from './arbeitsliste-model';

describe('ArbeitslisteModel', () =>
{
    it('should create an instance', () =>
    {
        expect(new ArbeitslisteModel()).toBeTruthy();
    });
});
